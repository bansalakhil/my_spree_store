development:
  api_key: "40db11000e1179fa4a597cae9a9867da"
  api_password: "d4aa91efc7a7463256b60aa41fa05a0b"
  store_domain: "bakhil2.myshopify.com"
  per_page: 250
  wait_time: 0.5

  # Default shipping category to be assigned for imported products
  shipping_category: "Shopify Products" 
  import_product_images: true

  # set true,  if, all option types global. Means Shoe sizes(7, 8, 9 etc) and Jeans sizes(36, 38 etc) will map under same option.
  # set false, if product_type specific option types. In this case shopify's product_type will be prepended like: Shirt > Size. Presentation will be Size only
  global_option_types: true

  # if True, duplicate sku will be set to blank. Else raise exception
  adjust_duplicate_sku: true
